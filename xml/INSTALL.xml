<?xml version='1.0'?>
<!DOCTYPE article PUBLIC "-//OASIS//DTD Simplified DocBook XML V1.1//EN"
                         "http://www.oasis-open.org/docbook/xml/simple/1.1/sdocbook.dtd">
<!-- $Id: INSTALL.xml,v 1.5 2008/01/14 23:32:04 jjg Exp jjg $ -->
<article>
<title>vfplot source installation</title>

<section>
<title>Executive summary</title>

<para>
vfplot is quite standard in its configuration, the usual
</para>

<blockquote>
<programlisting>
    ./configure
    make
    su
    make install
</programlisting>
</blockquote>

<para>
Use the <userinput>--prefix</userinput> option to 
<userinput>./configure</userinput> if you want to install
elsewhere than <filename>/usr/local</filename>.
</para>

</section>

<section>
<title>Prerequisites</title>

<para>
Although not required for compilation, the 
<ulink url="http://www.unidata.ucar.edu/software/netcdf/">NetCDF</ulink> 
library is needed if vfplot is to read 
GMT grd files. At present this is the main way
to get data into vfplot.
</para>

<para>
If you want support for 
<ulink url="http://gfs.sourceforge.net/">Gerris</ulink> 
flow solver use the <userinput>--enable-gerris</userinput> 
option in configuration. You will need to have a recent 
version of the Gerris software.
</para>

<para>
If the pthread (POSIX thread) library is found then 
vfplot will support multithreaded force calculatons 
giving a modest speedup on larger plots. 
</para>

<para>
See <userinput>./configure --help</userinput> for a full
list of configuration options.
</para>

</section>

<section>
<title>Portability</title>

<para>
vfplot should compile on any modern POSIX operating
system with a C99 compiler. It is developed on Linux
on the x86, AMD64 and Sparc architectures with the GNU 
C compiler and the Intel C++ compiler.
</para>

</section>

</article>

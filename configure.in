dnl Process this file with autoconf to produce a configure script.
dnl $Id: configure.in,v 1.15 2007/06/21 20:35:13 jjg Exp jjg $

AC_INIT(src/vfplot/main.c)

MAJOR_VERSION=0
MINOR_VERSION=6
VERSION="$MAJOR_VERSION.$MINOR_VERSION"

dnl Checks for programs.

AC_PROG_CC

if eval "test $GCC"; then
  CFLAGS="$CFLAGS -Wall"
fi

AC_PROG_MAKE_SET
AC_PROG_INSTALL

dnl AC_PATH_PROG(GSLCONFIG,gsl-config)
AC_PATH_PROG(GGO,gengetopt)

dnl Checks for header files.

AC_HEADER_STDC

dnl Checks for library functions.

AC_CHECK_LIB(m,sin)
AC_CHECK_LIB(cunit,CU_add_test)

dnl Output subsitutions

AC_SUBST(INSTALL_PROGRAM)
AC_SUBST(INSTALL_DATA)
AC_SUBST(VERSION)

dnl Make a config.h

AC_CONFIG_HEADER(src/include/config.h)

dnl Create Makefiles

AC_OUTPUT([Makefile src/Common.mk])







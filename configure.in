dnl Process this file with autoconf to produce a configure script.
dnl $Id: configure.in,v 1.2 2002/11/17 20:04:06 jjg Exp jjg $

AC_INIT(src/main.c)

MAJOR_VERSION=0
MINOR_VERSION=02
VERSION="$MAJOR_VERSION.$MINOR_VERSION"

dnl Checks for programs.

AC_PROG_CC
AC_PROG_MAKE_SET
AC_PROG_INSTALL

AC_PATH_PROG(GSLCONFIG,gsl-config,no)
if test "$GSLCONFIG" = no; then
  AC_MSG_WARN(no gsl-config)       
fi

AC_PATH_PROG(GGO,gengetopt)

dnl If we are using gcc the use some extra flags

if eval "test x$CC = xgcc"; then
  CFLAGS="$CFLAGS -Wall -O3 -g"
fi

dnl Checks for header files.

AC_HEADER_STDC
AC_CHECK_HEADER(gsl/gsl_spline.h)

dnl Checks for library functions.

AC_CHECK_LIB(m,sin)

dnl Output subsitutions

AC_SUBST(INSTALL_PROGRAM)
AC_SUBST(INSTALL_DATA)
AC_SUBST(VERSION)

dnl Make a config.h

AC_CONFIG_HEADER(src/config.h)

dnl Create Makefiles

AC_OUTPUT([Makefile src/Common.mk src/options.ggo])







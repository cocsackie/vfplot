dnl Process this file with autoconf to produce a configure script.
dnl $Id: configure.in,v 1.5 2002/10/29 21:09:56 jjg Exp $

AC_INIT(src/main.c)

MAJOR_VERSION=0
MINOR_VERSION=01
VERSION="$MAJOR_VERSION.$MINOR_VERSION"

dnl Checks for programs.

AC_PROG_CC
AC_PROG_MAKE_SET
AC_PROG_INSTALL

AC_PATH_PROG(TROFF, groff troff nroff,no)
if test "$TROFF" = no; then
  AC_MSG_WARN(No troff typesetter found!)       
fi

dnl If we are using gcc the use the -Wall option

if eval "test x$CC = xgcc"; then
  CFLAGS="$CFLAGS -Wall"
fi

dnl Checks for header files.

AC_HEADER_STDC
AC_CHECK_HEADER(gsl/gsl_spline.h)

dnl Checks for library functions.

AC_CHECK_FUNCS(strdup)

dnl libraries

AC_CHECK_LIB(gsl,gsl_spline_alloc,,)

dnl Output subsitutions

AC_SUBST(INSTALL_PROGRAM)
AC_SUBST(INSTALL_DATA)
AC_SUBST(VERSION)

dnl Make a config.h

AC_CONFIG_HEADER(src/config.h)

dnl Create Makefiles

AC_OUTPUT([Makefile src/Makefile src/options.ggo])







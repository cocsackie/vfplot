/*
  macros.h

  a few utility preprocessor macros

  J.J.Green 2007
  $Id$
*/

#ifndef MACROS_H
#define MACROS_H

#define MAX(a,b) ((a)>(b) ? (a) : (b))
#define MIN(a,b) ((a)<(b) ? (a) : (b))

#endif

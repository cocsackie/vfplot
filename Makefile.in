# Makefile.in for vfplot

# $Id: Makefile.in,v 1.2 2002/10/29 23:57:33 jjg Exp $
# help older versions of make

@SET_MAKE@

# Macros

VERSION   = @VERSION@
RUBBISH   = *~
CONFFILES = config.cache config.log config.status configure Makefile

# targets

default : all

all : 
	cd src ; $(MAKE) all ; cd ..

install : all
	cd src ; $(MAKE) install ; cd ..

clean :
	$(RM) $(RUBBISH)
	cd src ; $(MAKE) clean ; cd ..

veryclean : clean
	$(RM) $(CONFFILES)
	autoconf
	cd src ; $(MAKE) veryclean ; cd ..

dist : veryclean
	cd .. ; make vfplot.tar.gz ; cd vfplot

maint-dist : clean
	cd .. ; \
	tar -zcvf vfplot-maint.tar.gz vfplot ; \
	cd vfplot



